name: Release

on:
  push:
    tags: ['v*']

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm
      # No registry-url on purpose: it writes
      # //registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN} into .npmrc, and a
      # configured (retired) token is preferred over the OIDC exchange - the
      # registry then answers 404 on PUT instead of letting npm do trusted
      # publishing. Observed on the v0.2.15 tag of dsh-draw.

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Gate
        run: |
          pnpm test
          pnpm run check:readmes
          pnpm run verify:self-contained
          pnpm run verify:artifacts

      - name: Verify the changelog names this version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          grep -q "## \[${VERSION}\]" CHANGELOG.md || { echo "CHANGELOG.md has no '## [${VERSION}]' section"; exit 1; }

      # Trusted publishing: the OIDC token (id-token: write above) replaces the
      # long-lived NPM_TOKEN, so this step reads no secret. The family's older
      # userconfig/token step goes with it - and so does its "skip when the
      # secret is empty" branch, which turned a missing publisher into a silent
      # no-op. npm is upgraded first: the runner ships npm 10.x and the OIDC
      # exchange needs >= 11.5.1.
      - name: Upgrade npm (OIDC needs >= 11.5.1)
        run: |
          npm install -g npm@^11.5.1
          echo "npm: $(npm --version)"
          if [ -n "${ACTIONS_ID_TOKEN_REQUEST_TOKEN:-}" ]; then echo "OIDC request TOKEN: present"; else echo "OIDC request TOKEN: MISSING"; fi

      - name: Publish to npm
        env:
          # npm's OIDC exchange never throws: a failure is only visible at
          # verbose level, otherwise the step fails with ENEEDAUTH and no reason.
          NPM_CONFIG_LOGLEVEL: verbose
        run: |
          version="$(node -p "require('./package.json').version")"
          if npm view "dsh-plugin-upgrade-016@${version}" version >/dev/null 2>&1; then
            echo "dsh-plugin-upgrade-016@${version} is already published; skipping npm publish"
          else
            npm publish --provenance --access public
          fi

  release:
    needs: publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - name: Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "${{ github.ref_name }}" >/dev/null 2>&1; then
            echo "release ${{ github.ref_name }} already exists; skipping"
            exit 0
          fi
          VERSION="${GITHUB_REF_NAME#v}"
          node scripts/changelog-section.mjs "$VERSION" > release-notes.md
          if [ -s release-notes.md ]; then
            gh release create "${{ github.ref_name }}" \
              --title "${{ github.ref_name }}" \
              --notes-file release-notes.md
          else
            gh release create "${{ github.ref_name }}" --generate-notes
          fi
