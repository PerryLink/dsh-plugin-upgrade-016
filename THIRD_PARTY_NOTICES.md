# Third-Party Notices

This package bundles no third-party source code. Runtime and build dependencies are installed from the npm registry:

- `@deepseek-ai/schemastery` (peer) — Config schema, Apache-2.0-style upstream licensing per its package metadata.
- `@deepseek-ai/dsh-skill` (peer/dev) — the skill registry service this bundle registers into.
- `@deepseek-ai/cordis` (peer/dev) — the composition context.

The scanner (`lib/scan-0.1.6.mjs`) imports only Node.js standard library modules.

License: Apache-2.0. See `LICENSE`.
